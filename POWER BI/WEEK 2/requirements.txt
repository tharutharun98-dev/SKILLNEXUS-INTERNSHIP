# ---------------------------------------------------------------
# Week 2 – Data Modeling & Relationships | Requirements
# ---------------------------------------------------------------
# 1) Main tool (not installable via pip):
#      Microsoft Power BI Desktop (free, Windows) – latest version
#      https://powerbi.microsoft.com/desktop/
#
# 2) Optional Python helpers (dataset generator + validation/diagram)
#      Python 3.9 or newer
#      Install with:  pip install -r requirements.txt
# ---------------------------------------------------------------
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
openpyxl>=3.1
